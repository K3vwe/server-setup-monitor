# Configuration file 

# Git User Information
GIT_USER="K"
GIT_EMAIL="rukkimax@gmail.com"


# Log files to monitor
LOG_FILES=(
    "/var/log/syslog"
    "/var/log/auth.log"
    "/var/log/kern.log"
    "/var/log/users.log"
    "/var/log/monitor.log"

)

# Error patterns or keywords to look for in the logs
ERROR_PATTERNS=(
    "ERROR"
    "CRITICAL"
    "FAILURE"
    "WARNING"
)

# Directory to store processed logs
PROCESSED_LOG_DIR="/var/log/processed_logs"

# Notification method (e.g., email, slack)
NOTIFICATION_METHOD="email"
NOTIFICATION_EMAIL="...@example.com"
# Slack webhook URL (if using Slack for notifications)
SLACK_WEBHOOK_URL="https://hooks.slack.com/services/your/webhook/url"
# Frequency of log checks (in minutes)
CHECK_FREQUENCY=10 # every 10 minutes

# End of configuration file
